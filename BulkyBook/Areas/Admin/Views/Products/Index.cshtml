@model IEnumerable<Product>
@{
    ViewData["Title"] = "Products";
}

<partial name="_Notifications" />

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">@ViewData["Title"]</h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="Products" asp-action="Upsert" class="btn btn-primary">
                <i class="bi bi-plus-circle"></i> &nbsp; Create New Product
            </a>
        </div>
    </div>
    <br /><br />
    <table id="products" class="display" style="width:100%">
        <thead>
            <tr>
                <th class="col-2">Title</th>
                <th class="col-2">Author</th>
                <th class="col-2">Category</th>
                <th class="col-2">ISBN</th>
                <th class="col-1">Price</th>
                <th class="col-3"></th>
            </tr>
        </thead>
        @*<tbody>
            @foreach (var product in Model)
            {
                <tr>
                    <td>
                        @product.Title
                    </td>
                    <td class="text-center">
                        @product.Author
                    </td>
                    <td class="text-center">
                        @product.Category
                    </td>
                    <td class="text-center">
                        @product.ISBN
                    </td>
                    <td class="text-center">
                        @product.Price
                    </td>
                    <td class="text-center">
                        <div class="btn-group" role="group">
                            <a asp-controller="Products" asp-action="Upsert" asp-route-id="@product.Id" class="btn btn-primary mx-2"><i class="bi bi-pencil-square"></i> Edit</a>
                            <a class="btn btn-danger mx-2 js-delete" data-product-id="@product.Id"><i class="bi bi-trash3"></i> Delete</a>
                        </div>
                    </td>
                </tr>
            }
        </tbody>*@
    </table>
</div>

@section Scripts
    {
    <script src="~/js/product.js"></script>
}